<%= form_with(model: doctors_reservation) do |form| %>
  <% if doctors_reservation.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(doctors_reservation.errors.count, "error") %> prohibited this doctors_reservation from being saved:</h2>

      <ul>
        <% doctors_reservation.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
